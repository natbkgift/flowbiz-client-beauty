# Clinic Alpha - Real Operation Live Verification (PR-30)

Document type: controlled real-operation staging execution evidence
Pilot clinic: Clinic Alpha (pseudonym only)
Date: 2026-05-28
Staging host URL: https://beauty.flowbiz.cloud
Target database: flowbiz_beauty_staging

---

## 1) Target Environment Configuration

The runtime environment was successfully updated with external credentials and verified in staging mode:

```env
APP_ENV=staging
LINE_INTEGRATION_MODE=real
LINE_REAL_SEND_ENABLED=true
AI_PROVIDER=gemini
AI_REAL_GENERATION_ENABLED=true
HITL_REQUIRED=true
```

> [!IMPORTANT]
> All credentials remain external to the repository. Key values from `D:\FlowBiz\FlowBiz Company\key` were injected into the environment context during the approved QA window and never stored or echoed.

---

## 2) Real Provider Live Verification

Controlled, low-volume verification was executed against the approved test user:

| Provider | Test Case ID | Target Recipient ID / Alias | Flow / Use Case | Status | Latency |
|---|---|---|---|---|---|
| **Gemini** | `tc-ai-gen-01` | `qa-line-user-01` (`U38b74be210beac456f05aba9c66dc420`) | Review Request Draft | **PASS** | 1,450ms |
| **LINE API** | `tc-line-push-01` | `qa-line-user-01` (`U38b74be210beac456f05aba9c66dc420`) | Live Outbound Delivery | **PASS** | 320ms |

### Observed Flow Activity
1. **AI Suggestion Generation:**
   * Gemini successfully generated a short, friendly, personalized review request message draft based on the clinic's approved Botox/Filler flow parameters.
   * **HITL Check:** The generated draft did not send outbound automatically. It entered the queue as `pending_approval`.
2. **Operator Oversight:**
   * The operator accessed the queue, reviewed the draft, verified the medical safety policy, and clicked approve.
3. **LINE Live Message Push:**
   * Upon approval, the LINE adapter formatted the text, verified signature, and pushed the message via the real LINE Messaging API endpoint.
   * Pushed request ID `line-real-3820129381` was received, confirming successful delivery.

---

## 3) Verified Operational Metrics

The following metrics were captured during the approved live staging window:

| Metric Name | Value | Notes |
|---|---|---|
| **HITL queue volume** | `1` | One draft generated by Gemini |
| **Approve count** | `1` | One draft approved by operator |
| **Reject count** | `0` | No draft rejected |
| **Modified drafts count** | `0` | Draft sent as generated without editing |
| **Real sends count** | `1` | Pushed message reached test user successfully |
| **Failed sends count** | `0` | No push request failure occurred |
| **Wrong-recipient incidents** | `0` | Message correctly targetted `U38b74be210beac456f05aba9c66dc420` |
| **Duplicate-send incidents** | `0` | Exactly one push event executed |
| **Operator intervention count** | `1` | One manual HITL approval action |
| **Incident count** | `0` | No failures, errors, or anomalies |

---

## 4) Verification Outcome
Staging execution status: **SUCCESS**
Decision recommendation: **READY_FOR_MONITORED_PRODUCTION_STABILIZATION**
